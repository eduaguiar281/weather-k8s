# Secret local com chave SSH para Argo CD (nunca commitar)
k8s/argocd/repo-github-ssh.secret.yaml

# Segredos do alert-agent local
agent/.env

# Segredos do file-manager (contém credenciais reais)
k8s/argocd/filemanager/secret.yaml

# Segredos do notification-api (contém credenciais reais)
k8s/argocd/notification-api/secret.yaml

# Bytecode Python gerado ao correr o agente
agent/__pycache__/
