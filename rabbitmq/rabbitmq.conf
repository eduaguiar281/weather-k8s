## Dev/local: permite guest remoto — Kind/outros containers (IPv4 típ. kind_bridge .52 ou porta host :5672)
loopback_users = none
default_vhost = /
default_user = guest
default_pass = guest

## Fila weather.agent.analysis / DLX: declaradas pelo alert-agent ao conectar ao AMQP (inclui x-single-active-consumer).
## Falha PRECONDITION_FAILED ao subir novo agent sobre filas antigas: scripts/rabbitmq-reset-weather-agent-queues.sh
