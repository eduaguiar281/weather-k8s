{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Agent: uvicorn (debug)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["main:app", "--host", "127.0.0.1", "--port", "8000"],
      "cwd": "${workspaceFolder}/agent",
      "envFile": "${workspaceFolder}/agent/.env",
      "justMyCode": false
    }
  ]
}
